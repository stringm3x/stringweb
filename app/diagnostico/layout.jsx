export const metadata = {
  title: "Diagnóstico STRING",
  robots: "noindex, nofollow",
};

export default function DiagnosticoLayout({ children }) {
  return <div className="bg-black min-h-screen">{children}</div>;
}
